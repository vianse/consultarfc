<%= form_for(@sesione) do |f| %>
  <% if @sesione.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sesione.errors.count, "error") %> prohibited this sesione from being saved:</h2>

      <ul>
      <% @sesione.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_user %><br>
    <%= f.text_field :id_user %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
