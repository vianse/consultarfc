
				<!-- begin navbar-right -->
				<% if user_signed_in? %>
				
					<% else %>

					<li>
						<%= link_to "Sign up", new_user_registration_path, :class => 'navbar-link'  %> |
				  		<%= link_to "Login", new_user_session_path, :class => 'navbar-link'  %>
					</li>
				</ul>
				<% end %>
	
<h1>Descarga los Movimientos del Sat</h1>
<p>Solo registrate y exporta tus movimientos.</p>
